# TODO

## std/path

- [ ] - create a pathBuf/pathBuilder type that can be used to build paths
        but doesn't do string concatenation until build() or toString() is called.
- [ ] - create a pathSlice type that can be used instead of string | URL for function
        overloads.
